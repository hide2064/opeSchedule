# A-1: 担当者（アサイン）管理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** タスクに担当者を割り当て、ガントバーにイニシャル表示・TaskDetail に担当者ドロップダウン・担当者フィルターを提供する。

**Architecture:** DB に `members` テーブルと `tasks.assignee_id` を追加。バックエンドに CRUD ルーターを追加。フロントエンドは ScheduleScreen でメンバーを fetch し GanttChart / TaskDetailPanel へ渡す。ProjectModal でメンバー管理 UI を提供。

**Tech Stack:** Python/FastAPI, SQLAlchemy, Alembic, pytest, React/JSX, Vite

---

## Task 1: バックエンド — Member モデル・スキーマ・マイグレーション

**Files:**
- Create: `backend/app/models/member.py`
- Create: `backend/app/schemas/member.py`
- Create: `backend/alembic/versions/0012_add_members_and_task_assignee.py`
- Modify: `backend/app/models/project.py`
- Modify: `backend/app/models/task.py`

- [ ] **Step 1: Member ORM モデルを作成**

新規ファイル `backend/app/models/member.py`:

```python
from sqlalchemy import ForeignKey, Integer, String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.database import Base


class Member(Base):
    __tablename__ = "members"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    project_id: Mapped[int] = mapped_column(
        Integer, ForeignKey("projects.id", ondelete="CASCADE"), nullable=False, index=True
    )
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    color: Mapped[str] = mapped_column(String(7), default="#888888")
    email: Mapped[str | None] = mapped_column(String(200), nullable=True)

    project: Mapped["Project"] = relationship("Project", back_populates="members")  # type: ignore[name-defined]  # noqa: F821
```

- [ ] **Step 2: Member スキーマを作成**

新規ファイル `backend/app/schemas/member.py`:

```python
from pydantic import BaseModel, field_validator

from app.schemas.base import OrmModel


class MemberCreate(BaseModel):
    name: str
    color: str = "#888888"
    email: str | None = None

    @field_validator("color")
    @classmethod
    def validate_color(cls, v: str) -> str:
        if not v.startswith("#") or len(v) != 7:
            raise ValueError("color must be a hex color like #4A90D9")
        return v


class MemberUpdate(BaseModel):
    name: str | None = None
    color: str | None = None
    email: str | None = None

    @field_validator("color")
    @classmethod
    def validate_color(cls, v: str | None) -> str | None:
        if v is not None and (not v.startswith("#") or len(v) != 7):
            raise ValueError("color must be a hex color like #4A90D9")
        return v


class MemberResponse(OrmModel):
    id: int
    project_id: int
    name: str
    color: str
    email: str | None
```

- [ ] **Step 3: Project モデルに members relationship を追加**

`backend/app/models/project.py` の末尾（`annotations` relationship の後）に追加:

```python
    members: Mapped[list["Member"]] = relationship(  # type: ignore[name-defined]  # noqa: F821
        "Member", back_populates="project", cascade="all, delete-orphan"
    )
```

- [ ] **Step 4: Task モデルに assignee_id を追加**

`backend/app/models/task.py` に以下を追加。

import に `Member` を追加（ファイル先頭の import `from app.database import Base` の後）:

`Task` クラスの `notes` フィールドの後に追加:

```python
    assignee_id: Mapped[int | None] = mapped_column(
        Integer, ForeignKey("members.id", ondelete="SET NULL"), nullable=True
    )
    assignee: Mapped["Member | None"] = relationship("Member")  # type: ignore[name-defined]
```

※ `Member` の import は循環参照を避けるため文字列参照のまま型アノテーションのみ使用する。

- [ ] **Step 5: Alembic マイグレーションを作成**

新規ファイル `backend/alembic/versions/0012_add_members_and_task_assignee.py`:

```python
"""add members table and task assignee_id

Revision ID: 0012
Revises: 0011
Create Date: 2026-05-02
"""
from alembic import op
import sqlalchemy as sa

revision = '0012'
down_revision = '0011'
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        'members',
        sa.Column('id', sa.Integer(), primary_key=True, autoincrement=True),
        sa.Column('project_id', sa.Integer(),
                  sa.ForeignKey('projects.id', ondelete='CASCADE'), nullable=False),
        sa.Column('name', sa.String(100), nullable=False),
        sa.Column('color', sa.String(7), nullable=False, server_default='#888888'),
        sa.Column('email', sa.String(200), nullable=True),
    )
    op.create_index('ix_members_project_id', 'members', ['project_id'])
    op.add_column('tasks', sa.Column(
        'assignee_id', sa.Integer(),
        sa.ForeignKey('members.id', ondelete='SET NULL'), nullable=True
    ))


def downgrade() -> None:
    op.drop_column('tasks', 'assignee_id')
    op.drop_index('ix_members_project_id', table_name='members')
    op.drop_table('members')
```

- [ ] **Step 6: alembic upgrade を実行**

```
cd backend
py -m alembic upgrade head
```

Expected: `Running upgrade 0011 -> 0012` が表示される

---

## Task 2: バックエンド — Members ルーター + main.py 登録

**Files:**
- Create: `backend/app/routers/members.py`
- Modify: `backend/app/routers/__init__.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: Members ルーターを作成**

新規ファイル `backend/app/routers/members.py`:

```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.database import get_db
from app.models.member import Member
from app.models.project import Project
from app.schemas.member import MemberCreate, MemberResponse, MemberUpdate
from app.utils import apply_patch, commit_and_refresh, get_or_404

router = APIRouter(tags=["members"])


def _check_member_in_project(member: Member, project_id: int) -> None:
    if member.project_id != project_id:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Member not found")


@router.get("/projects/{project_id}/members", response_model=list[MemberResponse])
def list_members(project_id: int, db: Session = Depends(get_db)) -> list[Member]:
    get_or_404(db, Project, project_id, "Project not found")
    return db.query(Member).filter(Member.project_id == project_id).order_by(Member.id).all()


@router.post(
    "/projects/{project_id}/members",
    response_model=MemberResponse,
    status_code=status.HTTP_201_CREATED,
)
def create_member(
    project_id: int, payload: MemberCreate, db: Session = Depends(get_db)
) -> Member:
    get_or_404(db, Project, project_id, "Project not found")
    if not payload.name.strip():
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Member name cannot be empty")
    member = Member(
        project_id=project_id,
        name=payload.name.strip(),
        color=payload.color,
        email=payload.email,
    )
    db.add(member)
    return commit_and_refresh(db, member)


@router.patch("/projects/{project_id}/members/{member_id}", response_model=MemberResponse)
def update_member(
    project_id: int, member_id: int, payload: MemberUpdate, db: Session = Depends(get_db)
) -> Member:
    get_or_404(db, Project, project_id, "Project not found")
    member = get_or_404(db, Member, member_id, "Member not found")
    _check_member_in_project(member, project_id)
    apply_patch(member, payload)
    return commit_and_refresh(db, member)


@router.delete("/projects/{project_id}/members/{member_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_member(
    project_id: int, member_id: int, db: Session = Depends(get_db)
) -> None:
    get_or_404(db, Project, project_id, "Project not found")
    member = get_or_404(db, Member, member_id, "Member not found")
    _check_member_in_project(member, project_id)
    db.delete(member)
    db.commit()
```

- [ ] **Step 2: __init__.py に members を追加**

`backend/app/routers/__init__.py` を以下に変更:

```python
from app.routers import annotations, comments, config, import_export, members, projects, snapshots, tasks

__all__ = ["annotations", "comments", "config", "import_export", "members", "projects", "snapshots", "tasks"]
```

- [ ] **Step 3: main.py に members router を登録**

`backend/app/main.py` の `app.include_router(comments.router, ...)` の後に追加:

```python
app.include_router(members.router, prefix="/api/v1")
```

また、import 行を変更:

```python
from app.routers import annotations, comments, config, import_export, members, projects, snapshots, tasks
```

---

## Task 3: バックエンド — Task スキーマ更新 + テスト

**Files:**
- Modify: `backend/app/schemas/task.py`
- Create: `backend/tests/test_members.py`

- [ ] **Step 1: TaskResponse に assignee フィールドを追加**

`backend/app/schemas/task.py` の import に追加:

```python
from __future__ import annotations
```

ファイル先頭の import 群に以下を追加（`from app.schemas.base import OrmModel` の後）:

```python
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from app.schemas.member import MemberResponse
```

`TaskResponse` クラスの末尾に追加:

```python
    assignee_id: int | None = None
    assignee: MemberResponse | None = None
```

- [ ] **Step 2: テストファイルを作成**

新規ファイル `backend/tests/test_members.py`:

```python
import pytest


@pytest.fixture
def project(client):
    r = client.post("/api/v1/projects", json={"name": "Test Project"})
    return r.json()


def test_list_members_empty(client, project):
    pid = project["id"]
    res = client.get(f"/api/v1/projects/{pid}/members")
    assert res.status_code == 200
    assert res.json() == []


def test_create_member(client, project):
    pid = project["id"]
    res = client.post(f"/api/v1/projects/{pid}/members", json={"name": "Alice", "color": "#FF5722"})
    assert res.status_code == 201
    data = res.json()
    assert data["name"] == "Alice"
    assert data["color"] == "#FF5722"
    assert data["project_id"] == pid


def test_create_member_blank_name_fails(client, project):
    pid = project["id"]
    res = client.post(f"/api/v1/projects/{pid}/members", json={"name": "  "})
    assert res.status_code == 400


def test_update_member(client, project):
    pid = project["id"]
    mid = client.post(f"/api/v1/projects/{pid}/members", json={"name": "Bob"}).json()["id"]
    res = client.patch(f"/api/v1/projects/{pid}/members/{mid}", json={"name": "Robert", "email": "r@example.com"})
    assert res.status_code == 200
    assert res.json()["name"] == "Robert"
    assert res.json()["email"] == "r@example.com"


def test_delete_member(client, project):
    pid = project["id"]
    mid = client.post(f"/api/v1/projects/{pid}/members", json={"name": "Carol"}).json()["id"]
    res = client.delete(f"/api/v1/projects/{pid}/members/{mid}")
    assert res.status_code == 204
    assert client.get(f"/api/v1/projects/{pid}/members").json() == []


def test_delete_member_sets_task_assignee_null(client, project):
    """メンバー削除時に担当タスクの assignee_id が NULL になることを確認する。"""
    pid = project["id"]
    mid = client.post(f"/api/v1/projects/{pid}/members", json={"name": "Dave"}).json()["id"]
    tid = client.post(f"/api/v1/projects/{pid}/tasks", json={
        "name": "Task A", "start_date": "2026-06-01", "end_date": "2026-06-05",
        "assignee_id": mid,
    }).json()["id"]
    # メンバー削除
    client.delete(f"/api/v1/projects/{pid}/members/{mid}")
    # タスクの assignee_id が NULL になっていること
    task = client.get(f"/api/v1/projects/{pid}/tasks").json()
    task = next(t for t in task if t["id"] == tid)
    assert task["assignee_id"] is None


def test_member_not_found(client, project):
    pid = project["id"]
    assert client.get(f"/api/v1/projects/{pid}/members").status_code == 200
    assert client.delete(f"/api/v1/projects/{pid}/members/99999").status_code == 404
```

- [ ] **Step 3: TaskCreate/TaskUpdate に assignee_id を追加**

`backend/app/schemas/task.py` の `TaskCreate` クラスに追加（`dependency_ids` フィールドの前）:

```python
    assignee_id: int | None = None
```

`TaskUpdate` クラスにも同様に追加:

```python
    assignee_id: int | None = None
```

- [ ] **Step 4: テストを実行**

```
cd backend
py -m pytest tests/test_members.py -v
```

Expected: `test_delete_member_sets_task_assignee_null` 以外はすべて PASS（assignee_id は Task router で処理するため最後に通す）

- [ ] **Step 5: 全テストを実行**

```
cd backend
py -m pytest tests/ -v
```

Expected: 全テスト PASSED

- [ ] **Step 6: コミット**

```
git add backend/app/models/member.py backend/app/schemas/member.py \
        backend/app/routers/members.py backend/app/routers/__init__.py \
        backend/app/main.py backend/app/models/project.py \
        backend/app/models/task.py backend/app/schemas/task.py \
        backend/alembic/versions/0012_add_members_and_task_assignee.py \
        backend/tests/test_members.py
git commit -m "feat: add members CRUD API and task assignee_id"
```

---

## Task 4: フロントエンド — api.js + ScheduleScreen

**Files:**
- Modify: `frontend/src/api.js`
- Modify: `frontend/src/components/schedule/ScheduleScreen.jsx`

- [ ] **Step 1: api.js にメンバー API を追加**

`frontend/src/api.js` の `// ── Projects ─` セクションの後、Tasks セクションの前に追加:

```js
// ── Members ──────────────────────────────────────────────
export const listMembers   = (pid)          => request('GET',    `/projects/${pid}/members`);
export const createMember  = (pid, data)    => request('POST',   `/projects/${pid}/members`, data);
export const updateMember  = (pid, mid, d)  => request('PATCH',  `/projects/${pid}/members/${mid}`, d);
export const deleteMember  = (pid, mid)     => request('DELETE', `/projects/${pid}/members/${mid}`);
```

- [ ] **Step 2: ScheduleScreen でメンバーを fetch して GanttChart へ渡す**

`frontend/src/components/schedule/ScheduleScreen.jsx` を変更:

`const [tasks, setTasks]` の state 宣言群に追加:

```jsx
const [members, setMembers] = useState([]);
```

単体モードの fetch 部分（`const [proj, taskList] = await Promise.all([api.getProject(pid), api.listTasks(pid)]);` の後）に追加:

```jsx
          const memberList = isMultiMode ? [] : await api.listMembers(pid).catch(() => []);
          setMembers(memberList);
```

`<GanttChart` コンポーネントの props に追加:

```jsx
members={members}
onMembersChange={setMembers}
```

---

## Task 5: フロントエンド — GanttChart フィルター + GanttBars バッジ

**Files:**
- Modify: `frontend/src/components/schedule/GanttChart.jsx`
- Modify: `frontend/src/components/schedule/GanttBars.jsx`

- [ ] **Step 1: GanttChart に members props とフィルター state を追加**

`GanttChart` コンポーネントのシグネチャを変更:

```jsx
export default function GanttChart({ tasks, project, config, projectTitle, isMultiMode, currentPid, onTasksChange, historySnap, onShowHistory, onExitHistory, pendingChanges, onMutation, onVersionUp, members = [], onMembersChange }) {
```

`showShiftDialog` state の後に追加:

```jsx
const [filterAssignee, setFilterAssignee] = useState(null); // null=全員
```

`displayTasks` の `useMemo`（searchQuery フィルターの後）に追加：

現在の `useMemo`:
```jsx
  }, [baseTasks, searchQuery]);
```

これを以下に変更（filterAssignee を追加）:

```jsx
  const displayTasks = useMemo(() => {
    let result = baseTasks;
    if (searchQuery.trim()) {
      const q = searchQuery.trim().toLowerCase();
      result = result.filter(t =>
        (t.name            || '').toLowerCase().includes(q) ||
        (t.category_large  || '').toLowerCase().includes(q) ||
        (t.category_medium || '').toLowerCase().includes(q) ||
        (t.notes           || '').toLowerCase().includes(q)
      );
    }
    if (filterAssignee !== null) {
      result = result.filter(t => t.assignee_id === filterAssignee || t._isSep);
    }
    return result;
  }, [baseTasks, searchQuery, filterAssignee]);
```

- [ ] **Step 2: ツールバーに担当者フィルターを追加**

検索フィールドの直後（`{!isMultiMode && (<input type="search" ... />)}` の後）に追加:

```jsx
        {!isMultiMode && members.length > 0 && (
          <select
            className="gantt-assignee-filter"
            value={filterAssignee ?? ''}
            onChange={e => setFilterAssignee(e.target.value === '' ? null : Number(e.target.value))}
            title="担当者フィルター"
          >
            <option value="">全員</option>
            {members.map(m => (
              <option key={m.id} value={m.id}>{m.name}</option>
            ))}
          </select>
        )}
```

- [ ] **Step 3: GanttBars に members prop を追加してバーにイニシャルを表示**

`GanttBars` の `export default function GanttBars` シグネチャに `members = []` を追加:

```jsx
export default function GanttBars({ tasks, groupedTasks, criticalTaskIds, chartStart, pxPerDay, isMultiMode, onTaskClick, onDragEnd, members = [] }) {
```

`GanttBars` の中で membersMap を作成（`const today = fmtDate(new Date());` の後）:

```jsx
  const membersMap = Object.fromEntries(members.map(m => [m.id, m]));
```

`GanttBar` の `onTaskClick` props の後に `member` props を追加:

```jsx
              <GanttBar
                ...
                member={task.assignee_id ? membersMap[task.assignee_id] : null}
              />
```

`GanttBar` の関数シグネチャに `member` を追加:

```jsx
function GanttBar({ task, left, width, isCritical, isMultiMode, pxPerDay, onDragEnd, onTaskClick, todayStr, member }) {
```

`gantt-bar__label` div の後に追加:

```jsx
        {member && width >= 24 && (
          <div
            className="gantt-bar__assignee"
            style={{ background: member.color }}
            title={member.name}
          >
            {member.name.charAt(0)}
          </div>
        )}
```

- [ ] **Step 4: GanttBars 呼び出し側で members を渡す**

`GanttChart.jsx` 内の `<GanttBars` に追加:

```jsx
members={members}
```

---

## Task 6: フロントエンド — TaskDetailPanel 担当者ドロップダウン + ProjectModal メンバー管理

**Files:**
- Modify: `frontend/src/components/schedule/TaskDetailPanel.jsx`
- Modify: `frontend/src/components/top/ProjectModal.jsx`

- [ ] **Step 1: TaskDetailPanel に担当者ドロップダウンを追加**

`TaskDetailPanel` の props に `members = []` を追加:

```jsx
export default function TaskDetailPanel({ task, allTasks, currentPid, criticalTaskIds, isMultiMode, anchorEl, onClose, onUpdated, onDeleted, onOpenComments, commentCount, members = [] }) {
```

`form` state の初期値に追加:

```jsx
    assignee_id: task.assignee_id ?? null,
```

`handleSubmit` の `data` オブジェクトに追加:

```jsx
      assignee_id: form.assignee_id,
```

フォーム内の「色」フィールドの後、「メモ」フィールドの前に追加:

```jsx
        <div className="form-row">
          <label className="form-label">担当者</label>
          <select
            className="form-input"
            value={form.assignee_id ?? ''}
            onChange={e => setForm(f => ({ ...f, assignee_id: e.target.value === '' ? null : Number(e.target.value) }))}
            disabled={isMultiMode}
          >
            <option value="">（未設定）</option>
            {members.map(m => (
              <option key={m.id} value={m.id}>{m.name}</option>
            ))}
          </select>
        </div>
```

- [ ] **Step 2: GanttChart で TaskDetailPanel に members を渡す**

`<TaskDetailPanel` の props に追加:

```jsx
members={members}
```

- [ ] **Step 3: ProjectModal にメンバー管理 UI を追加**

`ProjectModal.jsx` を変更。import に追加:

```jsx
import * as api from '../../api.js';
```
（すでに import されているため変更不要）

`const [loading, setLoading]` の後に追加:

```jsx
  const [members, setMembers] = useState([]);
  const [newMemberName, setNewMemberName] = useState('');
  const [newMemberColor, setNewMemberColor] = useState('#4A90D9');
```

既存プロジェクトを開く場合のメンバー fetch。`useEffect` として追加（`const showToast = useToast();` の後）:

```jsx
  useEffect(() => {
    if (!isNew && project?.id) {
      api.listMembers(project.id).then(setMembers).catch(() => {});
    }
  }, [isNew, project?.id]);
```

フォームの末尾（`form-actions` の前）にメンバー管理セクションを追加:

```jsx
        {!isNew && (
          <div className="form-row form-row--members">
            <label className="form-label">メンバー</label>
            <div className="member-list">
              {members.map(m => (
                <div key={m.id} className="member-item">
                  <span className="member-item__badge" style={{ background: m.color }}>
                    {m.name.charAt(0)}
                  </span>
                  <span className="member-item__name">{m.name}</span>
                  <button
                    type="button"
                    className="member-item__del"
                    onClick={async () => {
                      if (!confirm(`「${m.name}」を削除しますか？`)) return;
                      await api.deleteMember(project.id, m.id);
                      setMembers(prev => prev.filter(x => x.id !== m.id));
                    }}
                  >✕</button>
                </div>
              ))}
              <div className="member-add-row">
                <input
                  type="color"
                  className="member-add-color"
                  value={newMemberColor}
                  onChange={e => setNewMemberColor(e.target.value)}
                />
                <input
                  className="form-input member-add-name"
                  placeholder="名前を入力して追加"
                  value={newMemberName}
                  onChange={e => setNewMemberName(e.target.value)}
                  onKeyDown={async (e) => {
                    if (e.key !== 'Enter' || !newMemberName.trim()) return;
                    e.preventDefault();
                    const m = await api.createMember(project.id, { name: newMemberName.trim(), color: newMemberColor });
                    setMembers(prev => [...prev, m]);
                    setNewMemberName('');
                  }}
                />
                <button
                  type="button"
                  className="btn btn--secondary btn--sm"
                  onClick={async () => {
                    if (!newMemberName.trim()) return;
                    const m = await api.createMember(project.id, { name: newMemberName.trim(), color: newMemberColor });
                    setMembers(prev => [...prev, m]);
                    setNewMemberName('');
                  }}
                >＋</button>
              </div>
            </div>
          </div>
        )}
```

---

## Task 7: CSS + ビルド + 最終コミット

**Files:**
- Modify: `frontend/src/styles/app.css`

- [ ] **Step 1: CSS を app.css 末尾に追加**

```css
/* ── Assignee ────────────────────────────────────────────── */
.gantt-bar__assignee {
  position: absolute;
  right: 3px;
  top: 1px;
  width: 15px;
  height: 15px;
  border-radius: 50%;
  font-size: 9px;
  font-weight: 700;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
  border: 1px solid rgba(255,255,255,.5);
}
.gantt-assignee-filter {
  height: 30px;
  padding: 0 8px;
  border: 1px solid var(--color-border);
  border-radius: 4px;
  background: var(--color-surface);
  color: var(--color-text);
  font-size: 13px;
  cursor: pointer;
  max-width: 120px;
}
/* ── Member Management ───────────────────────────────────── */
.member-list { display: flex; flex-direction: column; gap: 6px; width: 100%; }
.member-item { display: flex; align-items: center; gap: 8px; }
.member-item__badge { width: 24px; height: 24px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700; color: #fff; flex-shrink: 0; }
.member-item__name { flex: 1; font-size: 13px; }
.member-item__del { background: none; border: none; cursor: pointer; color: var(--color-text-muted); font-size: 13px; padding: 2px 4px; }
.member-item__del:hover { color: var(--color-danger); }
.member-add-row { display: flex; gap: 6px; align-items: center; margin-top: 4px; }
.member-add-color { width: 28px; height: 28px; border: none; padding: 0; cursor: pointer; border-radius: 4px; }
.member-add-name { flex: 1; }
```

- [ ] **Step 2: ビルド確認**

```
cd frontend
npm run build
```

Expected: ビルド成功（エラーなし）

- [ ] **Step 3: 全バックエンドテストを確認**

```
cd backend
py -m pytest tests/ -v
```

Expected: 全テスト PASSED

- [ ] **Step 4: 最終コミット & push**

```
git add frontend/src/api.js \
        frontend/src/components/schedule/ScheduleScreen.jsx \
        frontend/src/components/schedule/GanttChart.jsx \
        frontend/src/components/schedule/GanttBars.jsx \
        frontend/src/components/schedule/TaskDetailPanel.jsx \
        frontend/src/components/top/ProjectModal.jsx \
        frontend/src/styles/app.css
git commit -m "feat: add member management, task assignee, gantt badge, and assignee filter (A-1)"
git push origin main
```

---

## セルフレビュー

**Spec coverage:**
- `members` テーブル + `tasks.assignee_id` DB変更 ✅ Task 1
- CRUD API（GET/POST/PATCH/DELETE /members） ✅ Task 2
- メンバー削除時 SET NULL ✅ Task 3 test + migration
- TaskResponse に assignee フィールド ✅ Task 3
- ProjectModal でメンバー管理 ✅ Task 6
- TaskDetailPanel 担当者ドロップダウン ✅ Task 6
- ガントバーイニシャルバッジ ✅ Task 5
- 担当者フィルター ✅ Task 5

**Placeholder scan:** なし ✅

**Type consistency:**
- `MemberResponse` は Task 1 で定義、Task 3 の TaskResponse と Task 6 で参照 ✅
- `members` prop は Task 4 ScheduleScreen → Task 5 GanttChart → Task 5 GanttBars の順で伝播 ✅
- `filterAssignee` は Task 5 Step 1 で追加、Step 2 で参照 ✅
- `form.assignee_id` は Task 6 Step 1 で追加、handleSubmit で参照 ✅
